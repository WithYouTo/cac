<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qcap.cac.dao.AttenceMapper">

    <select id="listAttence" resultType="java.util.Map" parameterType="com.qcap.cac.dto.AttenceSearchParam">
        SELECT
        ATTENCE_ID attenceId,
        ORG_NAME orgName,
        PERSON_ID personId,
        WORK_NO workNo,
        PERSON_NAME personName,
        PERSON_MOBILE personMobile,
        DATE_FORMAT(ATTENCE_TIME,'%Y-%m-%d %H:%i:%S') attenceTime,
        REMARK remark
        FROM TB_ATTENCE
        WHERE 1=1
        <if test="workNo != null and workNo != ''">
            and WORK_NO = #{workNo}
        </if>
        <if test="personName != null and personName != ''">
            and PERSON_NAME = #{personName}
        </if>
        <if test="attenceTime != null and attenceTime != ''">
            and ATTENCE_TIME = #{attenceTime}
        </if>
    </select>
</mapper>
