<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qcap.cac.dao.EventTaskMapper">

  <insert id="insertFlightInfo" parameterType="com.qcap.cac.entity.TbFlightInfo">
    insert into tb_flight_info (FLIGHT_INFO_ID, FLIGNT_NAME, EVENT_TYPE, 
      FLIGHT_TYPE, DEPARTURE_GATE, GUARANTEE_TYPE, 
      PLANNING_TAKEOFF_TIME, ESTIMATED_TAKEOFF_TIME, 
      AIRCRAFT_TYPE, REMARK1, REMARK2, 
      REMARK3, CREATE_EMP, CREATE_DATE, 
      UPDATE_DATE, UPDATE_EMP, VERSION
      )
    values (#{flightInfoId}, #{fligntName}, #{eventType}, 
      #{flightType}, #{departureGate}, #{guaranteeType}, 
      #{planningTakeoffTime}, #{estimatedTakeoffTime}, 
      #{aircraftType}, #{remark1}, #{remark2}, 
      #{remark3}, #{createEmp}, #{createDate}, 
      #{updateDate}, #{updateEmp}, #{version}
      )
  </insert>
  
  <select id="selectEventPlan" parameterType="Map" resultType="Map">
	  SELECT 
		m.PLAN_EVENT_ID  AS  planEventId,
		m.AREA_CODE  AS  areaCode,
		m.STANDARD_CODE  AS  standardCode,
		m.EVENT_TYPE  AS  eventType,
		m.GUARANTEE_TYPE  AS  guaranteeType 
		FROM tb_plan_event m
		WHERE AREA_CODE =#{areaCode} 
		AND EVENT_TYPE=#{eventType}   
		AND  GUARANTEE_TYPE =#{guaranteeType}
  </select>
</mapper>
