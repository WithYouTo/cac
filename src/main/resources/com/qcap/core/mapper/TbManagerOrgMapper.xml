<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qcap.core.dao.TbManagerOrgMapper">

    <select id="getManagerIdByOrgId" resultType="java.lang.String">
        SELECT m.manager_id
        FROM tb_manager_org m
                 left join tb_org o ON m.org_id = o.id
        WHERE m.org_id = #{orgId}
    </select>

    <select id="getOrgFullCodeByUserId" resultType="java.lang.String">
        SELECT o.code code
        FROM tb_manager_org m
        left join tb_org o ON m.org_id = o.id
        WHERE m.manager_id = #{id}
    </select>
</mapper>
